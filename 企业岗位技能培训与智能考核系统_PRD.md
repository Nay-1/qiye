# 《基于 Spring Boot + Spring AI + Vue 的企业岗位技能培训与智能考核系统》PRD

版本：V5.0（按四轮评审意见升级：单库部署 / question_skill 强制校验）  
产品类型：企业内部培训管理平台（PC Web 端）  
技术方向：Spring Boot + Spring AI + Vue  
目标用户：企业管理员、培训负责人、企业员工  
更新日期：2026-08-12

---

# 1. 产品概述

## 1.1 产品背景

企业员工技能培训多存在以下问题：
1. 培训内容分散，缺少统一管理平台；
2. 员工培训过程无法追踪；
3. 岗位技能要求与培训课程脱节（"学非所用"）；
4. 培训效果无法量化评估；
5. 员工遇到技术/制度问题时缺少即时辅助。

## 1.2 产品目标

建立企业内部**岗位能力培养闭环**，而非单纯的"课程 + 考试"管理：

```
岗位需求 → 技能体系 → 技能关联课程 → 员工学习 → 在线考核
→ 技能成绩计算 → 员工技能画像 → 薄弱技能 → 推荐内容 → AI 建议 → 培训优化
```

## 1.3 产品价值

- **对管理员**：统一管理培训资源，量化分析员工技能水平，降低管理成本；
- **对培训负责人**：按岗位技能精准建设课程与考核，追踪培训效果；
- **对员工**：按岗位学技能、参加考核、获得针对性学习反馈与即时辅助。

---

# 2. 用户角色分析

## 2.1 系统管理员
- 用户管理、部门管理、角色权限、系统配置。

## 2.2 培训负责人
- 维护岗位技能体系、创建课程、管理知识库、创建考试、查看培训数据。

## 2.3 企业员工
- 查看岗位学习任务、学习课程、参加考试、查看技能画像、使用 AI 助手。

---

# 3. 系统功能结构

```
企业岗位技能培训与智能考核系统
├── 用户权限管理
├── 岗位技能体系管理
├── 培训课程管理
├── 企业知识库管理
├── 在线考试管理
├── 学习过程管理
├── 员工技能画像
├── AI 智能培训助手
└── 数据统计分析
```

---

# 4. 核心业务主链（系统设计灵魂）

这是整个系统最核心的数据链，**所有数据库表、接口、页面都围绕它展开**：

```
              岗位(job)
                ↓
          岗位技能(job_skill, 含目标等级 target_level)
                ↓
      技能 → 课程 关联(course_skill, 含权重 weight / required)
                ↓
            学习任务 / 学习记录(study_record)
                ↓
       考试(exam + question + question_skill)
                ↓
        技能成绩计算（按题目-技能得分聚合）
                ↓
        员工技能画像(user_skill: current_level/score)
                ↓
     薄弱技能识别（画像 score vs target_level）
                ↓
          推荐学习内容（薄弱技能反向关联课程）
                ↓
          AI 学习建议（业务规则判定 + LLM 生成文案）
```

> 这条链一旦建立，系统就从"培训管理系统 + AI 功能"升级为真正的**岗位能力培养系统**。后续所有模块（含 AI）都是为这条链服务的。

---

# 5. 功能需求

## 5.1 用户权限管理模块

- 三角色：管理员 / 培训负责人 / 员工；
- JWT + Spring Security 认证；
- 员工与岗位关系明确（一名员工可有主岗位，可兼岗）。

### RBAC 权限矩阵

| 功能 | 管理员 | 培训负责人 | 员工 |
|---|---:|---:|---:|
| 用户 / 部门管理 | ✅ | ❌ | ❌ |
| 岗位技能管理 | ✅ | ✅ | ❌ |
| 课程管理 | ✅ | ✅ | ❌ |
| 知识库管理 | ✅ | ✅ | ❌ |
| 创建考试 / 组卷 | ✅ | ✅ | ❌ |
| 参加考试 | ❌ | ❌ | ✅ |
| 技能画像查看 | ✅ | ✅ | ✅（本人） |
| AI 问答 | ✅ | ✅ | ✅ |
| 数据分析 | ✅ | ✅ | 部分 |

## 5.2 岗位技能体系管理模块（核心）

- **岗位管理**：岗位分类、岗位描述；
- **技能管理**：技能名称、描述（技能自身不设等级，等级见岗位要求与员工画像）；
- **岗位—技能关联（job_skill）**：为岗位配置技能要求，并定义**目标等级 target_level** 与权重，形成"岗位技能模型"。

示例：
```
岗位：Java开发工程师
  ├─ Java基础      target_level=中级
  ├─ Spring Boot   target_level=高级
  ├─ Redis         target_level=中级
  └─ MySQL         target_level=高级
```

## 5.3 培训课程管理模块

- 课程分类（技术/管理等）、难度、适用岗位；
- 章节管理（章节标题、内容、知识点）；
- **课程—技能关联（course_skill）**：声明"本课程培养哪个技能"，含权重与是否必修；
- 学习进度记录（章节级，含学习时长与完成状态）。

## 5.4 企业知识库管理模块（AI 基础）

- 文档上传（PDF / Word / TXT），**限制文件大小与类型**；
- 处理流程：上传 → 文本解析（PDFBox/POI）→ 切片 → 向量化 → 存入 **PostgreSQL + pgvector**；
- 知识库文档可按部门/岗位**隔离权限**，AI 回答仅检索授权范围。

## 5.5 在线考试管理模块

- 题库管理（单选/多选/判断，含答案、解析）；
- **题目—技能关联（question_skill）**：声明每道题考核哪个技能及分值权重；
- 组卷（exam_question）：选题、分值、考试时间、考试次数；
- 在线考试：进入 → 答题 → 提交 → 自动评分 → 成绩与历史记录（exam_attempt / exam_answer）；
- 每题得分按关联技能聚合，供技能画像计算。

## 5.6 学习过程管理模块

- 按岗位生成学习任务/计划（training_task）；
- 章节级学习记录：进度、学习时长、完成状态、开始/完成时间；
- 学习数据沉淀，供分析与 AI 建议使用。

## 5.7 员工技能画像模块（核心，答辩亮点）

- **user_skill**：记录每位员工在每项技能上的 current_level / score / target_level；
- **技能得分 score**：该技能关联题目的实得分 / 该技能关联题目满分（由 `question_skill` 聚合计算），取值 0~100；
- **等级换算规则（score → current_level）**：
  - score ≥ 80 → 高级（target_level=高级 判定达标）；
  - 60 ≤ score < 80 → 中级（target_level=中级 判定达标；target_level=高级 判定未达标）；
  - score < 60 → 初级（仅 target_level=初级 判定达标）；
- **达成率公式**：
  ```
  达成率 = 技能得分 / 目标等级达标线 × 100%   （达成率 ≥100% 取 100% 封顶）
  达标线：初级 60 / 中级 60 / 高级 80
  ```
  示例：员工 target_level=高级、score=72 → 达成率 = 72 / 80 = **90%**（未达标，判为薄弱技能）。
- 可视化：**技能雷达图**、薄弱技能列表、**培训前 vs 培训后对比**。

## 5.8 AI 智能培训助手模块

### 功能 1：AI 培训问答（RAG）
```
用户问题 → 向量检索 → Top-K 文档片段 → Prompt → LLM → 回答 + 来源
```
**边界规则（必做）**：
- 检索不到相关内容时，**禁止假装从知识库找到答案**，明确回复"知识库暂未收录";
- 回答显示来源文档；
- 回答仅基于授权范围内的知识库文档（隔离）。

### 功能 2：AI 生成培训试题
- 培训负责人输入"根据《Spring Boot 课程》生成 5 道选择题"；
- AI 生成题目（题干/选项/答案/解析）→ **必须经人工审核确认**后才入题库；
- **审核时同步绑定 `question_skill`**（选择该题考核的技能与分值权重）——否则生成的题目无法进入"题目 → 技能 → 画像"聚合链，该功能等于白做；
- **强制校验**：前端 + 后端双重校验——人工审核保存时**必须至少选择一个技能及分值权重**，否则不允许提交入库，防止管理员漏绑导致题目无法进入画像计算。

### 功能 3：AI 学习建议（业务规则 + LLM 协作）
**不让大模型直接判断能力**，采用：
```
业务规则计算 → 员工技能画像 → 识别薄弱技能 → 推荐课程 → AI 生成自然语言建议
```
示例：
```
Redis：考试得分 56%，要求中级，判定薄弱，推荐《Redis缓存优化》
→ AI 文案：你的 Redis 基础掌握程度较低，建议优先学习缓存策略与缓存一致性章节。
```
- AI 建议仅作辅助，**不直接修改 user_skill**。

## 5.9 数据统计分析模块

- 员工学习情况（学习人数、完成率、时长）；
- 考试分析（平均分、通过率、历史成绩趋势）；
- 技能达成率 / 薄弱技能项 / 技能雷达图；
- 部门培训完成率、员工技能排名；
- ECharts 可视化。

---

# 6. 非功能需求

## 6.1 性能
- 支持 100+ 用户同时访问；
- 页面响应 < 3 秒。

## 6.2 系统可靠性
- API 统一异常处理；
- 操作与错误日志记录；
- 文件上传大小与类型限制；
- AI 请求超时处理、AI 调用失败**降级**（提示"AI 服务暂不可用"，不阻塞业务）。

## 6.3 数据安全
- 密码 BCrypt 加密存储；
- JWT 过期机制；
- 文件访问权限控制；
- 知识库数据按部门/岗位隔离；
- 敏感操作（考试、技能等级变更）记录日志。

## 6.4 AI 安全
- Prompt Injection 防护（对用户输入做约束/清洗）；
- 知识库权限隔离；
- 回答来源溯源；
- 模型调用异常处理与降级。

---

# 7. 数据库设计（完整版）

> 本版按评审意见补齐"岗位 → 技能 → 课程 → 学习 → 考试 → 技能画像"全链路关联。

## 用户表 user
| 字段 | 说明 |
|---|---|
| id | 主键 |
| username / password | 账号 / BCrypt 密码 |
| name | 姓名 |
| dept_id | 部门 |
| role_id | 角色 |

## 部门表 dept
| 字段 | 说明 |
|---|---|
| id | 主键 |
| name | 部门名称 |

## 岗位表 job
| 字段 | 说明 |
|---|---|
| id | 岗位ID |
| name | 岗位名称 |
| dept_id | 所属部门 |
| description | 岗位描述 |

## 技能表 skill
| 字段 | 说明 |
|---|---|
| id | 技能ID |
| name | 技能名称 |
| description | 技能说明 |

> **等级语义约定**：技能表自身**不设等级**。等级仅存在于两处——岗位要求等级 `job_skill.target_level`、员工当前等级 `user_skill.current_level`，避免三级（表级/岗位级/员工级）语义混乱。

## 岗位技能表 job_skill（核心）
| 字段 | 说明 |
|---|---|
| id | 主键 |
| job_id | 岗位 |
| skill_id | 技能 |
| target_level | 岗位要求的目标等级 |
| weight | 权重 |

## 员工岗位表 user_job（一名员工可有多岗位）
| 字段 | 说明 |
|---|---|
| id | 主键 |
| user_id | 员工 |
| job_id | 岗位 |
| is_primary | 是否主岗位 |

## 课程表 course
| 字段 | 说明 |
|---|---|
| id | 课程ID |
| name | 课程名称 |
| category | 分类 |
| level | 难度 |
| job_id | 适用岗位 |

## 课程技能表 course_skill（核心，课程↔技能）
| 字段 | 说明 |
|---|---|
| id | 主键 |
| course_id | 课程 |
| skill_id | 技能 |
| weight | 权重 |
| required | 是否必修 |

## 课程章节表 course_chapter
| 字段 | 说明 |
|---|---|
| id | 章节ID |
| course_id | 课程 |
| title | 章节标题 |
| content | 章节内容 |

## 学习记录表 study_record（章节级）
| 字段 | 说明 |
|---|---|
| id | 主键 |
| user_id | 员工 |
| course_id | 课程 |
| chapter_id | 章节 |
| progress | 学习进度 |
| study_duration | 学习时长 |
| status | 未学/学习中/完成 |
| started_at / completed_at | 开始 / 完成时间 |

## 培训任务表 training_task（一条任务 = 员工 × 技能 × 一门课程）
| 字段 | 说明 |
|---|---|
| id | 主键 |
| user_id | 员工 |
| job_id | 岗位 |
| skill_id | 目标技能 |
| course_id | 对应课程 |
| status | 待学/进行中/完成 |
| source | 来源（系统分配/AI推荐） |

> **任务生成规则**：岗位含多技能、每技能可对应多门课程，系统按"岗位 → 技能 → 课程"展开为多条任务记录。
> **多岗位等级合并规则**：员工挂多个岗位时，取各岗位对该技能 `target_level` 的**最高等级**作为画像目标，避免多个目标相互矛盾。

## 考试表 exam
| 字段 | 说明 |
|---|---|
| id | 考试ID |
| title | 考试名称 |
| course_id | 关联课程（可选） |
| duration | 时长 |
| attempts | 允许次数 |
| status | 草稿/发布/关闭 |

## 题库表 question
| 字段 | 说明 |
|---|---|
| id | 题目ID |
| type | 单选/多选/判断 |
| content | 题干 |
| options | 选项 |
| answer | 答案 |
| analysis | 解析 |

## 题目技能表 question_skill（核心，题↔技能）
| 字段 | 说明 |
|---|---|
| id | 主键 |
| question_id | 题目 |
| skill_id | 技能 |
| score_weight | 分值权重 |

## 试卷题目表 exam_question
| 字段 | 说明 |
|---|---|
| id | 主键 |
| exam_id | 试卷 |
| question_id | 题目 |
| score | 本题分值 |
| sort | 排序 |

## 考试记录表 exam_attempt
| 字段 | 说明 |
|---|---|
| id | 主键 |
| user_id | 员工 |
| exam_id | 试卷 |
| total_score | 总分 |
| status | 进行中/已提交 |
| started_at / submitted_at | 开始 / 提交时间 |

## 学生答案表 exam_answer
| 字段 | 说明 |
|---|---|
| id | 主键 |
| attempt_id | 考试记录 |
| question_id | 题目 |
| user_answer | 学生答案 |
| correct | 是否正确 |
| score | 本题得分 |

## 员工技能画像表 user_skill（核心）
| 字段 | 说明 |
|---|---|
| id | 主键 |
| user_id | 员工 |
| skill_id | 技能 |
| current_level | 当前等级 |
| target_level | 目标等级 |
| score | 技能得分（聚合计算） |
| updated_at | 更新时间 |

## 知识库文档表 knowledge_file（文件元数据）
| 字段 | 说明 |
|---|---|
| id | 文件ID |
| name | 文件名 |
| path | 地址 |
| dept_id | 可见部门（单部门隔离；多部门可见为后续可选） |

## 知识库切片表 knowledge_chunk（文档 → 多向量）
| 字段 | 说明 |
|---|---|
| id | 切片ID |
| file_id | 所属文档 |
| content | 切片文本 |
| seq | 切片顺序 |
| vector_id | 切片向量 |

> 一个文档经"解析 → 切片"产生多个 chunk，**每个 chunk 一个向量**；检索在 chunk 层进行，命中的 chunk 反查所属文档供溯源。`knowledge_file` 只存文件元数据，不直接挂向量。

---

# 8. 技术架构

```
        Vue3 + Element Plus + ECharts
                    │
               Axios（REST API）
                    │
              Spring Boot
                    │
          PostgreSQL + pgvector
     （业务数据 + 知识库向量，单库部署）
              Spring AI → 大模型 API
```

**重要说明**：
- 采用 **PostgreSQL + pgvector 单库部署**：业务表（用户/课程/考试/画像）与知识库向量（切片向量）存放在**同一数据库**，避免维护 MySQL + PostgreSQL 两个库带来的部署与事务同步复杂度；
- 若更熟悉 MySQL，也可**全量 MySQL**：知识库向量检索用 MySQL 8 的 `VECTOR` 类型或应用层余弦计算；
- 本阶段不引入 Redis（无高并发需求）。

**技术选型**
| 环节 | 方案 |
|---|---|
| 后端 | Spring Boot 3 + MyBatis-Plus + Spring Security + JWT |
| 前端 | Vue3 + Vite + Element Plus + ECharts |
| 数据库 | **PostgreSQL + pgvector 单库**（业务表 + 知识库向量同库；备选：全量 MySQL，向量检索用 MySQL8 VECTOR 或应用层计算） |
| 文档解析 | Apache PDFBox / POI |
| AI | Spring AI + 国产大模型（通义 qwen / DeepSeek） |

---

# 9. 创新点总结（答辩可用）

1. **"岗位—技能—课程—试题—画像"全链路数据模型**：从"传课程"升级为以岗位技能为导向的精准培养闭环，技能达成率可量化计算（这是本版最大升级）。
2. **员工技能画像与薄弱技能识别**：考试得分按题目-技能聚合，生成技能雷达图与"培训前后对比"，培训效果可视化。
3. **基于知识库的 AI 智能问答（RAG）**：回答可溯源，知识库按部门隔离，无结果时明确告知而非硬编。
4. **业务规则 + LLM 协作的 AI 学习建议**：能力判定交给规则引擎，LLM 只负责生成自然语言建议，稳定可靠。

---

# 10. 工作量评估

| 模块 | 难度 |
|---|---|
| 用户权限（RBAC） | ⭐⭐ |
| 岗位技能 / 课程 / 学习 | ⭐⭐⭐ |
| 在线考试（四表+评分） | ⭐⭐⭐ |
| 员工技能画像（聚合计算） | ⭐⭐⭐ |
| 企业知识库（RAG） | ⭐⭐⭐ |
| AI 助手 | ⭐⭐⭐ |
| 数据分析 | ⭐⭐ |

整体难度：**⭐⭐⭐（适合本科毕设）**。建议开发顺序：
```
权限 → 岗位技能 → 课程 → 学习记录 → 考试 → 技能画像 → 知识库/AI → 看板
```
先打通"岗位→技能→课程→考试→画像"核心链，再加 AI，避免功能堆砌。
